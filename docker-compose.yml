services:
  doc-builder:
    container_name: ${COMPOSE_PROJECT_NAME}-doc-builder
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${HTTP_PORT}:80"
    volumes:
      - "./output:/app/output"
    restart: unless-stopped
